import { SurveyController } from './survey.controller';

export class ServerRoutes {
  
  // static readonly BASE_URL = 'https://techtestapi1.azurewebsites.net';
  
  //controllers
    static readonly Survey = SurveyController;
}